# Backlog

Stories are listed in priority order (head = highest priority).
When a story reaches `done`, remove it from the backlog and renumber.

1. **STORY-006** — Surface loud failure when set_story_status target is not in backlog *(in-progress)*
2. **STORY-010** — Add check_acceptance_criterion tool
